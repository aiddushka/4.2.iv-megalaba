listener 1883

# Для мегалабы аутентификацию устройств делаем на уровне приложения
# (подпись сообщений HMAC в payload), а брокер оставляем без пароля.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout

